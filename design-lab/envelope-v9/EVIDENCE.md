# Envelope v9 evidence

Status: **Design Lab only / P3 rendered-target PASS / P4 extraction-kernel PASS / P5 standalone transformer PASS / P6 v8-independent fixture PASS / public GitHub profile NOT_RUN**

Envelope v9 remains a donor/portability experiment. The current public profile remains the direct-IPM surface. No v9 asset is promoted live by this work.

## P2 baseline

PR #56 merged Envelope v9 P2 as `ed11b1c01ed3506e1bb6d16563ba014da962681e`.

P2 established bounded `safe|native|minimal` text, `opaque|transparent` outer background, mounted-source `inherit|preserve`, motion/frame/cap controls, font-file-free deterministic vector text and fail-closed unsupported glyphs. P2 did not claim rendered typography, transparent light/dark behavior or playback.

## P3 rendered-target proof

PR #59 was cleaned to one commit, passed exact-head CI, and was squash-merged as `efa691dfc1f46d36b27541157181871b20385e98`.

Accepted clean P3 receipt:

- head `bb24385251dbb70efe42abcb81e7c8aef5ee75ea`;
- run `32771760679`;
- job `97573467038`;
- result: **SUCCESS**.

P3 binds evidence to actual generated bytes rather than config alone. The real-Chrome matrix covers nine cases across transparent/opaque, safe/native/minimal, light/dark, desktop/mobile, hero and Projects surfaces. Accepted markers:

- `ENVELOPE_V9_BROWSER_MATRIX_PASS cases=9 schemes=light/dark widths=desktop/mobile surfaces=hero/projects`;
- `TARGET_LAYOUT=PASS`;
- `TEXT_RENDER=PASS`;
- `TRANSPARENCY_RENDER=PASS`;
- `NATIVE_RENDER=OBSERVED`;
- `MINIMAL_DYNAMIC_TEXT=PASS`.

Safe transparent text is verified at approximately `rgb(31,35,40)` on light hosts and `rgb(240,246,252)` on dark hosts with contrast >= 12 in the tested GitHub-like backgrounds. Screenshot pixels verify transparent host-background exposure. Native remains explicitly host-font-dependent. Minimal suppresses dynamic visible labels while retaining semantic metadata.

P3 also proves bounded local playback:

- exact v8 -> v9 `.v7-motion` subtree equivalence across 15 generated assets;
- normal motion-on: 6.0s / 0.5s / 13 samples, left **7** changed pairs, right **12**;
- browser-wide reduced motion: left **0**, right **0**;
- motion-off: left **0**, right **0**.

`PUBLIC_GITHUB_PROFILE=NOT_RUN` remains intentional because v9 is not mounted live.

## P4 extraction readiness

PR #64 was cleaned to one commit, passed exact-head CI, and was squash-merged as `09c929a2224aa78610e528995db2fb595331359a`.

Accepted clean P4 receipt:

- head `03a8d29499594aacb3515688558705c47b2dd089`;
- full v9 run `32773284543`, job `97578374131`: **SUCCESS**;
- portable contract run `32773284888`, job `97578376096`: **SUCCESS**;
- render target `71797c18a43efc3aac75c6769f57284c1dda2a38d0685ad037f3f11e23009e94`.

P4 established a five-file standalone-copyable kernel and owner-independent Schema identity `urn:profile-envelope:config:v1`. Its extraction smoke found and fixed a real accessibility bug: encoded source text such as `&amp;` could be decoded for glyph geometry but escaped again from the encoded source for `aria-label` / `<title>`. The kernel now derives both from the same decoded visible string.

Accepted extraction marker:

- `ENVELOPE_V9_EXTRACTION_READINESS_PASS core_files=5 python_stdlib_only=true personalized_tokens=0 standalone_contract=PASS standalone_vector_text=PASS schema_alignment=PASS`.

## P5 adapter decoupling

PR #65 moved portable presentation policy out of the donor producer and was squash-merged as `6fb4bdf1ef33402ad5635ad188410d5dbed2eb51`.

New boundary:

- `github_profile_transform.py` — standalone-copyable GitHub-profile transformer; no v7/v8 or Project Map / Activity producer imports;
- `render_portable_surface.py` — thin donor producer that creates this repository's v8 bundle and delegates policy;
- donor bundle boundary — always preserves mounted-source backgrounds; the transformer alone owns `inherit|preserve`.

Accepted clean P5 receipt:

- base main `44a38610983720ee3fb91f9bb7fb22c81278746d`;
- head `8556064b5978ec16807aeaa98306994e4b037bb2` — one commit directly over main;
- full v9 run `32811886780`, job `97692641775`: **SUCCESS**;
- portable contract run `32811886796`, job `97692641886`: **SUCCESS**;
- render target `62d68a97a354ffc3c82efdbe2396730f5ca90cafdfa5579c59504b7df9c35b4f`.

The render-target SHA changed from the earlier P5 proof because Activity / Project Map-derived input bytes changed. The clean exact-head rerun re-earned browser and playback evidence instead of retaining a stale receipt.

Structural/extraction markers:

- `DONOR_BOUNDARY_EQUIVALENCE=PASS legacy_prestripped_vs_preserve_first=true`;
- `ENVELOPE_V9_EXTRACTION_TRANSFORM_PASS core_files=5 adapter_files=1 python_stdlib_only=true personalized_tokens=0 standalone_contract=PASS standalone_vector_text=PASS standalone_github_transform=PASS schema_alignment=PASS`.

The standalone extraction smoke copies the GitHub transformer into a temporary repository and applies it to a synthetic complete 15-SVG donor bundle. The same clean P5 exact head re-ran the nine-case Chrome matrix and motion proof successfully: motion-on left **7** / right **12**, reduced motion **0/0**, motion-off **0/0**, with exact inherited motion-subtree equivalence across 15 generated assets.

## P6 second-consumer fixture evaluation

PR #68 tested whether P5's standalone transformer still contained hidden donor identity assumptions and was squash-merged as `07c8f6845f5c28594371a1224ac44308dcf59b78`.

P6 found residual donor identity in the portable transformer:

- `v8-frame`;
- v8 surface-base IDs;
- Project Map's `galaxy-family-bg` identity;
- the Activity donor's exact background geometry/color.

Instead of introducing a universal IR, P6 narrowed the portable input boundary to three semantic SVG attributes:

- `data-profile-envelope-surface-base="outer"`;
- `data-profile-envelope-frame="rail"`;
- `data-profile-envelope-mounted-background="presentation"`.

The donor-bound producer translates current v8/Project Map/Activity identity into those markers. The portable transformer consumes only the markers and removes them before final output fingerprinting, so marker normalization does not become part of public rendered bytes.

Accepted clean P6 receipt:

- base main `1baf38898a89d7eb8df4fe431608b2c252ac2e26`;
- head `f4f6c60aad2010c1c81249e8f668b14ece834819` — one commit directly over main;
- full v9 run `32812597928`, job `97694710641`: **SUCCESS**;
- portable contract run `32812597971`: **SUCCESS**;
- render target remained `62d68a97a354ffc3c82efdbe2396730f5ca90cafdfa5579c59504b7df9c35b4f`.

Accepted P6 markers:

- `DONOR_BOUNDARY_EQUIVALENCE=PASS legacy_prestripped_vs_preserve_first=true`;
- `GENERIC_DONOR_MARKER_BOUNDARY=PASS portable_transformer_v8_identity=false`;
- `SECOND_CONSUMER_FIXTURE_PASS donor_identity=v8-independent geometry=heterogeneous generic_markers=3`;
- `ENVELOPE_V9_EXTRACTION_TRANSFORM_PASS core_files=5 adapter_files=1 python_stdlib_only=true personalized_tokens=0 standalone_contract=PASS standalone_vector_text=PASS standalone_github_transform=PASS schema_alignment=PASS`;
- `PUBLIC_REPO_CREATE=DEFERRED donor_producer=donor-bound second_consumer=FIXTURE_ONLY independent_consumer=NOT_ESTABLISHED new_skill=DEFERRED`.

The fixture differs from the v8 donor in dimensions, frame geometry, base colors and mounted-background identifiers. It contains no v8 element identity. The copied transformer successfully applies transparent/safe/inherit policy using only the three generic markers.

The current donor remains byte-equivalent under the marker refactor: the render-target fingerprint stayed identical to P5, the nine-case Chrome matrix passed, motion-on remained left **7** / right **12**, browser-wide reduced motion remained **0/0**, motion-off remained **0/0**, and exact v8 -> v9 motion-subtree equivalence remained 15 generated assets.

This proves a stronger adapter boundary, but the fixture is **evaluation evidence only**. It does not satisfy the publication gate requiring an independent external consumer or concrete reuse request.

## Negative proof lineage retained

Useful failed approaches remain evidence rather than being erased:

- asynchronous `--dump-dom` serialized before an async receipt was populated — harness race, not render evidence;
- `SVGSVGElement.setCurrentTime()` + `getCTM()` produced no useful timeline delta — replaced by persistent Chrome + real elapsed time + screenshot pixel diffs;
- parent-target-only CDP reduced-motion emulation did not establish propagation to separately decoded SVG images — browser-wide `--force-prefers-reduced-motion` is the accepted proof;
- standalone-copy P4 exposed accessibility double escaping that donor-local render tests had not discriminated — extraction must be executed, not inferred;
- the first P6 fixture assertion required an unused gradient definition to disappear after its marked background rectangle was removed. The presentation contract only requires removal of background paint; the over-strong test was corrected rather than expanding transformer scope to dead-def cleanup.

## Relationship to v8 public evidence

Envelope v8 has actual-public-profile evidence for 8 non-hero documents / 14 rail strips. v9 does not inherit that result wholesale. It proves exact inherited motion-subtree equivalence plus local v9 rendering/playback. Public v9 playback still requires intentionally mounting v9 on a stable public target and observing that target separately.

## Claim boundary

Established:

- deterministic target-sensitive generated-output fingerprint;
- real-Chrome light/dark and desktop/mobile rendering for the tested matrix;
- font-independent safe visible text for the supported glyph set;
- screenshot-backed transparent host-background exposure;
- local isolated rail playback, reduced-motion silence and motion-off silence;
- exact inherited motion-subtree equivalence to v8;
- five-file stdlib-only standalone core with owner-independent Schema identity;
- decoded accessibility metadata consistent with visible vector text;
- standalone-copyable GitHub-profile transformer operating on a declared complete 15-SVG donor bundle;
- byte-identical output across the old/new mounted-background donor boundary;
- a generic three-marker donor boundary with no v8/Project Map element identity in portable code;
- a v8-independent heterogeneous fixture that passes from the copied package tree.

Not established:

- v9 is live;
- public GitHub-profile v9 playback;
- shared runtime clocks or frame-perfect cross-document synchronization;
- universal typography quality for all future glyphs/data densities;
- arbitrary semantic source data can be converted into the 15-SVG donor bundle by the public package;
- an independent external second consumer exists;
- a standalone `profile-envelope` Skill is justified.

## Next safe action

P6 is merged. Further generic abstraction is not justified by current evidence. The next meaningful gates are packaging/license/release design and a real external reuse request or independent consumer. Public repository creation and a new standalone Skill remain deferred until those gates are met.
